typer>=0.12,<1.0
rich>=13.7,<16.0
pydantic>=2.6,<3.0
python-dotenv>=1.0,<2.0
ollama>=0.3.0,<1.0
GitPython>=3.1,<4.0
tree-sitter>=0.23,<1.0
tree-sitter-language-pack>=0.7,<2.0
chromadb>=0.5,<2.0
prompt_toolkit>=3.0,<4.0

# viva serve (Phase 10) -- previously missing from this file entirely,
# even though pyproject.toml's base dependencies always included them;
# `pip install -r requirements.txt` (this file, the README's documented
# path) left `viva serve` unrunnable until this fix. See
# docs/system-design/23-phase-18-dependency-auth-hygiene-design.md
# §23.1.2.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# dev/test
pytest>=8.0,<10.0
pytest-mock>=3.14,<4.0
# fastapi.testclient.TestClient needs this directly -- it was previously
# missing from this file entirely (pyproject.toml's dev extra already
# had it correctly). httpx2 is Pydantic's actively maintained fork of
# httpx, now Starlette's preferred TestClient dependency; plain httpx
# still works but is deprecated upstream. See docs/system-design/
# 23-phase-18-dependency-auth-hygiene-design.md §23.9.
httpx2>=2.0,<3.0
ruff>=0.16,<1.0
mypy>=2.0,<3.0
pytest-cov>=6.0,<8.0

# voice (Phase 11, docs/system-design/16-phase-11-voice-io-design.md) --
# optional; only needed if VOICE_ENABLED=true. Install via
# `pip install -e ".[voice]"` rather than this file if you only want
# voice support -- listed here too so a plain `pip install -r
# requirements.txt` user can find them.
# faster-whisper>=1.0,<2.0
# piper-tts>=1.3,<2.0
# sounddevice>=0.4,<1.0
# numpy>=1.26,<3.0
